# Mood Note Tool

This is a placeholder for a self-contained Card Tool package.

The package owns its local implementation files. The Card registers this package from `.nore/tools.json`, and Agents reference its capabilities through Card-level tool names such as `card.mood-note.write`.
